#!/usr/bin/env python3

class Parser:

    def parse_line(self, line: str) -> None:
        """ if len(line) == 0:
            return
        elif line[0] == '[' or line[0] == '{':
            return
        else:
            print(line) """
        print(line)
